import { CSSProperties } from "react";

export const s: Record<string, CSSProperties | ((active: boolean) => CSSProperties)> = {
  page: {
    minHeight: "100vh",
    background: "#060609",
    color: "#e4e4ec",
    fontFamily: "'Outfit','Noto Sans JP',sans-serif",
    position: "relative",
  },
  center: {
    maxWidth: 700,
    margin: "0 auto",
    padding: "48px 20px",
    minHeight: "100vh",
    display: "flex",
    flexDirection: "column",
    justifyContent: "center",
    position: "relative",
    zIndex: 2,
  },
  card: {
    background: "rgba(255,255,255,0.02)",
    border: "1px solid rgba(255,255,255,0.06)",
    borderRadius: 16,
    padding: "36px",
    backdropFilter: "blur(24px)",
    animation: "scaleIn 0.5s cubic-bezier(0.16,1,0.3,1)",
    position: "relative",
    overflow: "hidden",
  },
  scan: {
    position: "absolute",
    top: 0,
    left: 0,
    right: 0,
    height: "1px",
    background: "linear-gradient(90deg,transparent,rgba(74,222,128,0.3),transparent)",
    animation: "scanline 4s linear infinite",
    pointerEvents: "none",
  },
  tag: {
    display: "inline-flex",
    alignItems: "center",
    gap: 6,
    padding: "5px 14px",
    borderRadius: 6,
    fontSize: "0.72rem",
    fontWeight: 600,
    background: "rgba(74,222,128,0.08)",
    color: "#4ade80",
    letterSpacing: "0.12em",
    textTransform: "uppercase",
    marginBottom: 14,
    border: "1px solid rgba(74,222,128,0.12)",
  },
  h2: {
    fontSize: "1.6rem",
    fontWeight: 700,
    color: "#fff",
    marginBottom: 20,
    lineHeight: 1.35,
    letterSpacing: "-0.02em",
  },
  ta: {
    width: "100%",
    minHeight: 110,
    background: "rgba(255,255,255,0.03)",
    border: "1px solid rgba(255,255,255,0.08)",
    borderRadius: 10,
    color: "#e4e4ec",
    fontSize: "0.95rem",
    padding: "14px 16px",
    resize: "vertical" as const,
    outline: "none",
    fontFamily: "'Outfit','Noto Sans JP',sans-serif",
    lineHeight: 1.65,
    transition: "all 0.3s",
    boxSizing: "border-box" as const,
  },
  pri: {
    display: "inline-flex",
    alignItems: "center",
    gap: 8,
    padding: "13px 28px",
    fontSize: "0.92rem",
    fontWeight: 600,
    background: "linear-gradient(135deg,#4ade80,#06b6d4)",
    color: "#060609",
    border: "none",
    borderRadius: 10,
    cursor: "pointer",
    transition: "all 0.25s cubic-bezier(0.16,1,0.3,1)",
  },
  sec: {
    display: "inline-flex",
    alignItems: "center",
    gap: 8,
    padding: "13px 28px",
    fontSize: "0.92rem",
    fontWeight: 500,
    background: "transparent",
    color: "#9a9ab0",
    border: "1px solid rgba(255,255,255,0.08)",
    borderRadius: 10,
    cursor: "pointer",
    transition: "all 0.25s",
  },
  ghost: {
    display: "inline-flex",
    alignItems: "center",
    gap: 6,
    padding: "9px 18px",
    fontSize: "0.82rem",
    fontWeight: 500,
    background: "rgba(255,255,255,0.02)",
    color: "#6a6a80",
    border: "1px solid rgba(255,255,255,0.05)",
    borderRadius: 8,
    cursor: "pointer",
    transition: "all 0.25s",
  },
  prog: {
    width: "100%",
    height: 3,
    background: "rgba(255,255,255,0.04)",
    borderRadius: 2,
    marginBottom: 28,
    overflow: "hidden",
  },
  res: {
    background: "rgba(0,0,0,0.5)",
    border: "1px solid rgba(255,255,255,0.06)",
    borderRadius: 10,
    padding: "20px",
    fontFamily: "'JetBrains Mono',monospace",
    fontSize: "0.85rem",
    lineHeight: 1.75,
    whiteSpace: "pre-wrap" as const,
    maxHeight: 440,
    overflowY: "auto" as const,
    color: "#b8b8d0",
  },
  err: {
    marginTop: 8,
    padding: "10px 14px",
    borderRadius: 8,
    background: "rgba(239,68,68,0.08)",
    border: "1px solid rgba(239,68,68,0.2)",
    color: "#f87171",
    fontSize: "0.85rem",
    display: "flex",
    alignItems: "center",
    gap: 8,
  },
};

export const optStyle = (active: boolean): CSSProperties => ({
  padding: "14px 18px",
  borderRadius: 10,
  cursor: "pointer",
  transition: "all 0.25s cubic-bezier(0.16,1,0.3,1)",
  background: active ? "rgba(74,222,128,0.08)" : "rgba(255,255,255,0.02)",
  border: active
    ? "1px solid rgba(74,222,128,0.35)"
    : "1px solid rgba(255,255,255,0.06)",
  color: active ? "#4ade80" : "#b0b0c0",
  fontWeight: 500,
  fontSize: "0.9rem",
  display: "flex",
  alignItems: "center",
  gap: 10,
});

export const dotStyle = (active: boolean): CSSProperties => ({
  width: 8,
  height: 8,
  borderRadius: "50%",
  flexShrink: 0,
  background: active ? "#4ade80" : "transparent",
  border: active
    ? "2px solid #4ade80"
    : "2px solid rgba(255,255,255,0.15)",
  transition: "all 0.25s",
});
